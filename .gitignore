# dependencies
node_modules/

# build output
dist/
.next/
out/

# env
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# debug
npm-debug.log*

# test
TEST_CREDENTIALS.md

# Local database dumps
.backups/

# Uploaded files
#
# Product photographs and the pictures customers attach to import requests.
# These are somebody else's property and this repository is public, so a
# `git add -A` must never sweep them in. They are backed up by reset-db.sh
# alongside the pg_dump instead — see scripts/reset-db.sh.
apps/api/uploads/
