{
  "name": "motorcycle-parts-internal-system",
  "private": true,
  "version": "0.1.0",
  "description": "Internal operations system for the motorcycle parts importing business",
  "scripts": {
    "dev": "./scripts/dev.sh",
    "stop": "lsof -ti:3000,3001 | xargs -r kill -9 2>/dev/null; docker compose stop",
    "db:up": "docker compose up -d",
    "db:studio": "cd apps/api && npm run db:studio",
    "test": "npm run test:api && npm run test:web",
    "test:api": "cd apps/api && npx jest",
    "test:web": "cd apps/web && npx playwright test",
    "typecheck": "cd apps/api && npx tsc --noEmit && cd ../web && npx tsc --noEmit"
  }
}
